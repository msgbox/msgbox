MsgBox Binary
===========================

This is the main binary for running MsgBox.

It reads in a config file at runtime and spawns:

  * Relay
  * Submission Agent
  * (n) Incoming Workers
  * (n) Outgoing Workers